# Generator Service Environment Configuration

# Server Configuration
GENERATOR_SERVICE_PORT=8084
GIN_MODE=debug

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=generator_platform
DB_SSL_MODE=disable

# JWT Configuration
JWT_SECRET=low-code-platform-secret-key-2024
